# life

## Commands

  life add [--title] [--description]
  
  Adds a new event by default prompting the user for data. Optionally, the user could pass a title and description by flags.

  life review --from --to

  Makes a review of events in a period. By default, reviews the last day events.

